// Import Actions
import {  } from './CategoriaActions';

// Initial State
const initialState = {};

const CategoriaReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CategoriaReducer;
